#include "../include/queue.h"
#include <stdio.h>

int main() {
  char *s1 = "Hello";
  char *s2 = "bye bye";

  enqueue(s1);
  enqueue(s2);

  char buf[100];
  dequeue(buf);
  printf("%s\n", buf);
  dequeue(buf);
  printf("%s\n", buf);

  return 0;
}
