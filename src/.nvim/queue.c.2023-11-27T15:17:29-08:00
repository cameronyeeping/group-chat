#include "../include/queue.h"
#include <pthread.h>
#include <stdio.h>
#include <string.h>

int enqueue(char *to_copy) {
  strncpy(msg_buf[tail].str, to_copy, BUF_SIZE);
    
}

int dequeue(char *buf) {
  strncpy(buf, msg_buf[index].str, BUF_SIZE);
    head = (head + 1) % MAX_MSGS;
}
