#include "../include/queue.h"
#include <stdio.h>
#include <string.h>
int enqueue(char *str, int len) {}

int dequeue(char *str) {
  char toReturn[BUF_SIZE];
  strncpy(toReturn, str, BUF_SIZE);
  index++;
}
